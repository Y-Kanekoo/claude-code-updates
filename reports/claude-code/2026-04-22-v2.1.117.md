# Claude Code 更新レポート / v2.1.117

| リリース日 | 影響度 | 破壊的変更 | 変更記載 | 推奨アクション |
|---|---|---|---|---|
| 2026-04-22 | 中 | 公式リリースノート上の明示なし | あり | 次回更新時に確認 |

<!-- section:links -->
### 関連リンク
- [GitHub Release](https://github.com/anthropics/claude-code/releases/tag/v2.1.117)
- [差分 v2.1.116...v2.1.117](https://github.com/anthropics/claude-code/compare/v2.1.116...v2.1.117)
- [公式ドキュメント](https://docs.anthropic.com/ja/docs/claude-code)
- [変更履歴](https://docs.anthropic.com/ja/docs/claude-code/changelog)

<!-- section:summary -->
### 要約
- `CLAUDE_CODE_FORK_SUBAGENT` 設定によるフォークサブエージェントの外部ビルド有効化や、`/model` の改善、`/resume` コマンドの機能強化などが含まれる。

<!-- section:judgement -->
### 判定
- **影響度**: 中
- **破壊的変更**: 公式リリースノート上の明示なし
- **変更記載**: あり
- **推奨アクション**: 次回更新時に確認

<!-- section:highlights -->
### 先に押さえるポイント
- `CLAUDE_CODE_FORK_SUBAGENT` を使用してフォークサブエージェントを外部ビルドで有効化できる。
- `/model` の選択が再起動後も維持されるようになり、プロジェクトが異なるモデルをピンしていても選択が保持される。
- `/resume` コマンドで古いセッションを読み込み前に要約するオプションが追加された。

<!-- section:changes -->
### 変更内容

#### 新機能
- **フォークサブエージェントの外部ビルド有効化**
  - 関連: `CLAUDE_CODE_FORK_SUBAGENT`
  - 外部ビルドでフォークサブエージェントを有効化できるようになった。
- **Advisor Tool (実験的)**
  - 関連: `Advisor Tool`
  - 対話式の実験的なツールが追加された。

#### 改善
- **`/model` の改善**
  - 関連: `/model`
  - 選択が再起動後も維持されるようになった。
- **`/resume` コマンドの改善**
  - 関連: `/resume`
  - 古いセッションを読み込み前に要約するオプションが追加された。
- **プラグインのインストール改善**
  - 関連: `plugin install`
  - 既にインストールされているプラグインの依存関係を自動的にインストールするようになった。

#### バグ修正
- **Plain-CLI OAuth セッションの修正**
  - 関連: `Plain-CLI OAuth`
  - アクセストークンが期限切れになったときにセッションが死んでしまう問題を修正。
- **`WebFetch` の修正**
  - 関連: `WebFetch`
  - 大きな HTML ページでハングする問題を修正。
- **プロキシの修正**
  - 関連: `NO_PROXY`
  - プロキシ設定が正しく尊重されない問題を修正。

#### セキュリティ
- **OpenTelemetry のセキュリティ強化**
  - 関連: `OpenTelemetry`
  - カスタム/MCP コマンド名のログ出力を制限するようになった。

<!-- section:breaking_changes -->
### 破壊的変更
なし

<!-- section:impact -->
### 影響範囲
- Claude Code を使用しているすべてのユーザーに影響する。
- 特に、フォークサブエージェントや `/model`、`/resume` コマンドを使用しているユーザーに影響する。

<!-- section:recommended_action -->
### 推奨対応
- 次回の更新時に、フォークサブエージェントの設定と `/model`、`/resume` コマンドの使用状況を確認することを推奨する。

<!-- section:notes -->
### 補足
なし

---
<sub>自動生成 / Groq llama-3.3-70b-versatile 要約</sub>
